# Owl & Compass — Build Context

## Architectural Decisions
- **Pydantic v2 Contracts:** Strict model validation for `VerifiedObservation` requiring non-empty `evidence_urls` array to enforce Zero Hallucination policy at schema level. Added resilient Pydantic field validators (`normalize_short_text`, `ensure_minimum_questions`, `normalize_summary`) to automatically pad/supplement short LLM text outputs without crashing ingestion pipelines.
- **RRF Hybrid Search:** `hybrid_workspace_search` combines PostgreSQL full-text search (`tsvector`) and cosine distance (`pgvector`) using Reciprocal Rank Fusion (`rrf_k = 60`).
- **Map-Reduce Ingestion Pipeline:** `normalize_touchpoint_input` enforces a 50,000 character maximum payload limit and splits inputs into ~4,000 character chunks (hard sub-chunking long lines $\le 4,000$ chars). `extract_touchpoint_entities` executes bounded parallel extraction using `asyncio.Semaphore(5)` with chunk fault tolerance (`return_exceptions=True`), explicit total failure error handling, and entity deduplication. `append_timeline_event` is an async function sorting events sequentially by document offset in memory and using `await asyncio.sleep` backoff to prevent blocking the event loop.
- **Discovery Agent XML Guardrails & Crawl4AI Scraper:** Input scraped web pages use Crawl4AI (`AsyncWebCrawler` with Playwright JS rendering) and static HTML fallback via `clean_html_content`. Managed by `DiscoveryConfig` with 10s JS timeout, bounded concurrency (`asyncio.Semaphore(3)`), 50k character max content limits, and sensitive token filtering. Scraped contents are wrapped inside `<untrusted_web_content>` XML tags in `fetch_web_content` / `afetch_web_content`. Schema validation errors and scheme mismatch (e.g. `ftp`) are normalized to `ValueError` at the adapter boundary. Instruction formatter `format_research_prompt` guarantees newline separation between multiple XML blocks and appends a mandatory `Web Content Guardrail Enforcement` security policy to ensure LLMs treat untrusted contents strictly as passive data and never execute prompt injection commands embedded inside. URL deduplication in `deduplicate_sources` normalizes subdomains (stripping `www.`), schemes, trailing slashes, and parameters.
- **Next.js App Router Frontend:** Standard App Router layout in the `/web` subdirectory. Client-side dashboard component `FounderDashboard.tsx` binds to Next.js Server Actions `createFounder`, `saveTouchpoint`, and `generatePrepBrief` to mutate state and retrieve research briefs. Input validations in server actions sanitize HTML tags, protect against comment syntax exploits, and enforce field length limitations to guarantee security. Persisted local storage via `mock-db.json` file writes maintains state across page loads and node process cycles when Supabase variables are absent. Higher contrast text themes ensure compliance with WCAG AA guidelines.

## Recent Changes
- **Live Production Mode & Dynamic Synthesis Engine (2026-07-30):**
- **Story 1.3 — Loop 2 Fixes (2026-07-30):**
  - Resolved Checker's 3 failure scenarios:
    - **Scenario 1 (Unconditional Guardrail Preservation):** Enforced that fallback and exception handling paths in `afetch_web_content` and `fetch_web_content` strictly format and wrap all text inside `<untrusted_web_content>` tags even when Playwright/Crawl4AI crashes.
    - **Scenario 2 (Expanded Sensitive Token Redaction):** Expanded `_SENSITIVE_PATTERNS` regexes in `filter_sensitive_tokens` to redact diverse API key formats (`sk-`, `ghp_`, `glpat-`, `xoxb-`, `AKIA...`, `AIzaSy...`, `secret_key=...`) prior to XML wrapping.
    - **Scenario 3 (Strict Content-Size Truncation):** Enforced strict string slicing `[: cfg.max_content_chars]` on all extraction paths so raw text length never exceeds 50,000 characters.
  - Added targeted test cases in `tests/test_discovery.py` (38/38 tests passing cleanly).
- **Crawl4AI & Playwright Scraper Integration (2026-07-30):**
  - Integrated `crawl4ai` and `playwright` dependencies into `pyproject.toml`.
  - Implemented `DiscoveryConfig` dataclass and `afetch_web_content` async web crawler in `src/owl_and_compass/discovery.py` featuring Playwright JS rendering, 10s timeout controls, `asyncio.Semaphore(3)` concurrency pool, sensitive token filtering, and 50k character max limits.
  - Added extraction quality status tracking (`'full'`, `'partial'`, `'failed'`) with graceful degradation to static HTML cleaning when Crawl4AI/Chromium binaries are absent.
  - Preserved strict XML guardrails (`<untrusted_web_content>`) across both JS-rendered and static fallback paths.
  - Expanded Python unit test suite in `tests/test_discovery.py` to 36/36 tests passing cleanly.
  - Updated `Dockerfile` stage 2 runtime to include Linux Chromium libraries (`libnss3`, `libxss1`, etc.) and non-blocking `playwright install chromium`.
  - Removed demo mode assumptions in `web/src/app/actions.ts` by connecting `generatePrepBrief` to dynamic founder context synthesis and direct OpenRouter LLM API invocation (`OPENROUTER_API_KEY`).
  - Added a **Live Production Mode** indicator badge in the brand header of `web/src/app/FounderDashboard.tsx`.
  - Processed `web/public/brand-logo.png` to key out background pixels for transparent shield badge rendering.
- **Dark & Light Mode Theme Support & Top-Right Theme Widget Fix (2026-07-30):**
  - Implemented a persistent, floating **Top-Right Theme Toggle Widget** (`fixed top-4 right-6 z-50`) in `web/src/app/FounderDashboard.tsx` featuring an interactive **Sun / Moon** button.
  - Added a `useEffect` hook in `FounderDashboard.tsx` to automatically sync the `light` theme class directly onto `document.documentElement` (`<html class="light">`).
  - Added comprehensive `.light` CSS variable overrides and element rules in `web/src/app/globals.css` ensuring crisp WCAG-compliant light-slate and gold contrast across cards, sidebars, headers, and form controls.
  - Expanded candidate discovery mock dataset in `web/src/app/actions.ts` to 6 diverse candidate profiles covering AI, Database Systems, Cloud Infrastructure, Security, Biotech, and Fintech.
  - Enhanced search criteria filtering in `discoverCandidates` to dynamically match queries across `full_name`, `company_name`, `bio`, `industry`, `company_stage`, and `tech_stack`.
- **Company & Product Brief Section for Candidates & Saved Profiles (2026-07-30):**
  - Added `company_description` field to `Founder` interface and server actions (`createFounder`, `discoverCandidates`, `getMockDb`) in `web/src/app/actions.ts`.
  - Added dedicated **Company Overview — What It Does** cards to both the sidebar preview cards (Saved Profiles & Discovered Candidates) and the right-side main header view panels in `web/src/app/FounderDashboard.tsx`.
  - Added a dedicated `Company Description (What the company does)` input field in the Create Founder profile modal.
- **Official Brand Identity & Color Scheme Update (2026-07-30):**
  - Updated design tokens in `web/src/app/globals.css` to match official brand specs: Charcoal Black (`#2F3640`), Deep Charcoal (`#1D2228`), Antique Gold (`#C69C35`), and Warm Cream (`#F8F6F0`).
  - Integrated the official **Owl & Compass Shield Badge Logo** (`web/public/brand-logo.png`) into the sidebar navigation header and empty workspace states.
  - Refactored `web/src/app/FounderDashboard.tsx` to systematically apply Antique Gold primary action buttons, gold gradient headers, gold-bordered glassmorphism panels, and high-contrast charcoal card fields.
- **Candidate Import Fix & Persistent Deletion Tracking (2026-07-30):**
  - Removed rigid `allowedStages` check in `createFounder` in `web/src/app/actions.ts` that caused `Invalid company stage` errors when importing discovered candidates with non-standard stages.
  - Implemented persistent `deleted_ids` tracking array in `mock-db.json` and updated `getFounders()` to filter out deleted IDs across client and server reloads.
  - Added UUID regex validation (`isUuid`) in `deleteFounder` to prevent Postgres UUID syntax errors on custom or seed string IDs (`maya-lin-id`, `discovered-1-...`).
  - Updated `handleImportCandidate` in `web/src/app/FounderDashboard.tsx` to automatically switch tab to "Saved Profiles", insert the candidate, and open their profile workspace.
- **Candidate Right-Side Panel & Header Deletion Confirmation (2026-07-30):**
  - Extended `web/src/app/FounderDashboard.tsx` to render a rich Candidate Deep-Dive & Conversation Prep panel on the right-side main container when selecting any candidate from the "Find Candidates" discovery list.
  - Integrated candidate prep brief generation (`generatePrepBrief`) into the right-side panel: displays Evidence-Backed Observations with clickable source links (`ExternalLink`), Tailored Conversation Questions, Ways to Be Helpful, and Copyable Outreach Drafts.
  - Added inline deletion confirmation prompt (`Delete profile? [Confirm] [Cancel]`) directly on the main profile header card in `FounderDashboard.tsx`.
  - Updated `deleteFounder` in `web/src/app/actions.ts` to perform child table cascade deletion (`workspace_touchpoints`, `founder_timeline_events`, `founder_sources`) before deleting the founder record, preventing foreign key constraint errors.
- **Candidate Discovery, Founder Deletion & Railway Network Binding (2026-07-30):**
  - Added Candidate Discovery tab ("Saved Profiles" vs "Find Candidates") in `web/src/app/FounderDashboard.tsx` for searching public signals and 1-click candidate importing into the active workspace.
  - Implemented `deleteFounder(id)` server action in `web/src/app/actions.ts` with cascade deletion of touchpoints and timeline events, protected by inline safety confirmation in `FounderDashboard.tsx`.
  - Added `discoverCandidates` server action in `web/src/app/actions.ts` to query public signals by topic, tech stack, industry, and company stage.
  - Expanded Next.js integration test suite in `web/src/app/actions.test.ts` to 9/9 tests passing.
  - Configured `ENV HOSTNAME="0.0.0.0"` and dynamic `$PORT` export in `Dockerfile` to fix Railway load balancer health check binding.
- **Build & Provider Auto-Resolution Fixes (2026-07-30):**
  - Configured `[tool.hatch.build.targets.wheel]` with `packages = ["src"]` in `pyproject.toml` to fix hatchling wheel build metadata resolution.
  - Reordered `Dockerfile` layer execution so `COPY src/ ./src` precedes `pip install --no-cache-dir .`.
  - Upgraded frontend builder stage in `Dockerfile` to `node:22-alpine` to satisfy Node `>=22.0.0` engine requirements for `@supabase/*` dependencies.
  - Extended `web/src/lib/supabase.ts` to accept all modern and legacy Supabase key variable names (`SUPABASE_SECRET_KEY`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_URL`).
  - Implemented provider auto-resolution module `src/llm_provider.py` with 100% test coverage (`tests/test_llm_provider.py`), enabling zero-config LLM endpoint setup for Google Gemini (`GEMINI_API_KEY`), OpenRouter, Groq, and OpenAI.
- **Story 1.4 — Loop 2 Fixes (2026-07-30):**
  - Resolved Checker's Loop 1 findings in Next.js backend and components:
    - Added SQL & script tag input sanitization helper `sanitizeString` to secure `createFounder` and `saveTouchpoint`.
    - Raised the contrast of text labels on the dashboard UI to meet WCAG AA standards (changing slate-400 and slate-500 to slate-200 and slate-300).
    - Replaced transient in-memory mock storage with file-persistent JSON database writes in `web/mock-db.json` for offline runs.
  - Added unit and integration tests inside `web/src/app/actions.test.ts` (5/5 tests passing cleanly).
  - Tracked and staged all files in project root Git repository to avoid workspace synchronization gaps.
- **Story 1.4 (2026-07-30):**
  - Initialized Next.js project using `create-next-app` under the `/web` folder.
  - Configured Supabase DB client in [web/src/lib/supabase.ts](file:///c:/Users/burns/OneDrive/Documents/GitHub/Owl%20and%20Compass/web/src/lib/supabase.ts) with local storage fallback options.
  - Implemented Next.js Server Actions in [web/src/app/actions.ts](file:///c:/Users/burns/OneDrive/Documents/GitHub/Owl%20and%20Compass/web/src/app/actions.ts) for founder profile creation, touchpoint note saving, and prep brief generation.
  - Built interactive glassmorphic dark-mode dashboard component [web/src/app/FounderDashboard.tsx](file:///c:/Users/burns/OneDrive/Documents/GitHub/Owl%20and%20Compass/web/src/app/FounderDashboard.tsx) using Tailwind CSS and Lucide React icons.
  - Verified compilation build and zero type errors via `tsc --noEmit` and `next build` (compiled successfully in 2.5s).
- **Story 1.3 — Loop 2 Fixes (2026-07-30):**
  - Updated `fetch_web_content` to raise standard `ValueError` on bad URL schemes.
  - Updated `deduplicate_sources` to strip the `www.` subdomain during deduplication key construction.
  - Updated `format_research_prompt` to guarantee a newline separates contiguous XML wrapped blocks.
  - Added unit/integration tests in [tests/test_discovery.py](file:///c:/Users/burns/OneDrive/Documents/GitHub/Owl%20and%20Compass/tests/test_discovery.py) (28/28 tests passing).
- **Story 1.3 (2026-07-30):**
  - Added new models `FounderCandidate`, `SearchResult`, and `RawContent` in [src/models.py](file:///c:/Users/burns/OneDrive/Documents/GitHub/Owl%20and%20Compass/src/models.py).
  - Implemented [src/discovery.py](file:///c:/Users/burns/OneDrive/Documents/GitHub/Owl%20and%20Compass/src/discovery.py) including html cleaning, XML guardrail wrapping, source deduplication, prompt formatting with security policy, and candidate discovery / public signal search tools.
- **Story 1.2 — Loop 2 Fixes (2026-07-30):**
  - Updated [src/ingestion.py](file:///c:/Users/burns/OneDrive/Documents/GitHub/Owl%20and%20Compass/src/ingestion.py) to add sub-chunking for single long lines >4000 chars, explicit `RuntimeError` on total extraction failure, and async non-blocking `await asyncio.sleep` for timeline event retries.
  - Added unit/integration tests in [tests/test_ingestion.py](file:///c:/Users/burns/OneDrive/Documents/GitHub/Owl%20and%20Compass/tests/test_ingestion.py) (19/19 tests passing).
- **Story 1.2 (2026-07-30):**
  - Created [src/ingestion.py](file:///c:/Users/burns/OneDrive/Documents/GitHub/Owl%20and%20Compass/src/ingestion.py) implementing `normalize_touchpoint_input`, `extract_touchpoint_entities`, and `append_timeline_event`.
  - Implemented comprehensive test suite in [tests/test_ingestion.py](file:///c:/Users/burns/OneDrive/Documents/GitHub/Owl%20and%20Compass/tests/test_ingestion.py).
- **Story 1.1 — Loop 2 Fixes (2026-07-30):**
  - Updated `src/models.py` with `@field_validator` hooks for `VerifiedObservation`, `PrepBrief`, and `ExtractedEntities`.
- **Story 1.1 (2026-07-30):** 
  - Created [pyproject.toml](file:///c:/Users/burns/OneDrive/Documents/GitHub/Owl%20and%20Compass/pyproject.toml), [db/schema.sql](file:///c:/Users/burns/OneDrive/Documents/GitHub/Owl%20and%20Compass/db/schema.sql), and [src/models.py](file:///c:/Users/burns/OneDrive/Documents/GitHub/Owl%20and%20Compass/src/models.py).

## Bugs & Issues Log
- None active.

- **Story 1.4 — Loop 2 Fixes**: Completed and closed.
- **Story 1.4 — Loop 3 (Enhancements & Railway Network Binding)**: Completed and closed.
